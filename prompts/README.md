# Prompts

This folder is reserved for future LLM-backed bookmark enrichment.

V1 uses deterministic heuristics so the pipeline can run locally with zero setup beyond Python. When the bookmark artifact contract stabilizes, prompts here can replace or augment the current enrichment functions without changing storage.
